{
  "name": "agracta",
  "private": true,
  "description": "Agracta — PWA de ensaios de campo. NAO ha build: o app e servido como esta. Este arquivo existe so para o teste que precisa de um DOM de verdade.",
  "scripts": {
    "test": "bash conferir.sh",
    "test:regras": "firebase emulators:exec --project demo-agracta-integracoes --only firestore \"node tests/portal-rules.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.27.0",
    "jsdom": "^24.0.0",
    "fake-indexeddb": "^6.2.5"
  }
}
